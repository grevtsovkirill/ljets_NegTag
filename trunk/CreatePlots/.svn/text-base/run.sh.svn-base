#!/bin/bash

#[[ -d eps_plots ]] || mkdir eps_plots
#[[ -d pdf_plots ]] || mkdir pdf_plots
[[ -d png_plots ]] || mkdir png_plots

./runit.sh PlotCombined

#which pdfunite && pdfunite pdf_plots/*.pdf all_WPs.pdf
